@model Preschool.Models.Child


@{
    ViewData["Title"] = "ChildPage";
}

<h1>@Model.FirstName @Model.LastName Page</h1>


@*@foreach(var document in Model.DocumentsImage)
{

    // here use a carosal 
    <img src="~/DocumentsCopies/@document.ImageFile" width="500" height="700">
    <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
        <ol class="carousel-indicators">
            <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
            <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
            <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
        </ol>
        <div class="carousel-inner">
            <div class="carousel-item active">
                <img class="d-block w-100" src="..." alt="First slide">
            </div>
            <div class="carousel-item">
                <img class="d-block w-100" src="..." alt="Second slide">
            </div>
            <div class="carousel-item">
                <img class="d-block w-100" src="..." alt="Third slide">
            </div>
        </div>
        <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
}*@
@*
@foreach (var document in Model.DocumentsImage)
{
    <div id="carousel-@document.Id" class="carousel slide" data-ride="carousel">
        <ol class="carousel-indicators">
            @for (int i = 0; i < Model.DocumentsImage.Count; i++)
            {
                <li data-target="#carousel-@document.Id" data-slide-to="@i" @(i == 0 ? "class=active" : "")></li>
            }
        </ol>
        <div class="carousel-inner">

            <div class="carousel-item active">
                <img class="/DocumentsCopies/" src="~/DocumentsCopies/nedladdning.jpeg" width="700" height="auto" alt="First slide">
            </div>
            @foreach (var image in Model.DocumentsImage)
            {
                <div class="carousel-item">
                    <img src="~/DocumentsCopies/@image.ImageFile" width="700" height="auto"  alt="Image">
                </div>
            }
        </div>
        <a class="carousel-control-prev" href="#carousel-@document.Id" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#carousel-@document.Id" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
}*@

<div class="image-gallery">
    @foreach (var image in Model.DocumentsImage)
    {
        <div class="image-item">
            <img src="~/DocumentsCopies/@image.ImageFile" alt="@image.ImageFile">
        </div>
    }
</div>


@*Renew the supscription *@


@*subscirption history*@
<div>
    @await Component.InvokeAsync("ChildSubscriptionsHistory" , new {id = Model.Id})
</div>
    
  

@*Attendnec schema*@
